import { Box, Typography } from "@mui/material";

const Header = () => {
  return (
    <Box>
      <Typography variant="h1" gutterBottom>
        Todo App
      </Typography>
    </Box>
  );
};

export default Header;
